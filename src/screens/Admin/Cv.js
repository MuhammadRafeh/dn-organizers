import React from 'react';
import { View, Text } from 'react-native';

const Cv = props => {
    return (
        <View style={{flex: 1}}>
            <Text>
                Cv screen
            </Text>
        </View>
    );
}

export default Cv;